{{- if .Values.networkPolicy.enabled }}
# =============================================================================
# NetworkPolicy: user-service
# =============================================================================
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: user-service-network-policy
  namespace: fitforge-prod
  labels:
    app: user-service
    app.kubernetes.io/part-of: fitforge
spec:
  podSelector:
    matchLabels:
      app: user-service
  policyTypes:
    - Ingress
  ingress:
    # Allow from Envoy Gateway pods
    - from:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              app.kubernetes.io/name: envoy
      ports:
        - port: 5002
          protocol: TCP
{{- end }}
