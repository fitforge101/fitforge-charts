# =============================================================================
# SealedSecret: workout-service-secret   (SAFE TO COMMIT TO GIT)
# =============================================================================
# Secrets needed: MONGO_URI, JWT_SECRET
#
# HOW TO GENERATE:
#   1. kubectl create secret generic workout-service-secret \
#        --namespace fit-forge \
#        --from-literal=MONGO_URI="mongodb://workout_user:<PASSWORD>@mongodb-0.mongodb-headless.fit-forge.svc.cluster.local:27017,mongodb-1.mongodb-headless.fit-forge.svc.cluster.local:27017,mongodb-2.mongodb-headless.fit-forge.svc.cluster.local:27017/workout_db?replicaSet=rs0" \
#        --from-literal=JWT_SECRET="<YOUR_STRONG_JWT_SECRET>" \
#        --dry-run=client -o yaml > /tmp/workout-service-secret.yaml
#   2. kubeseal --format yaml --controller-name sealed-secrets-controller \
#        --controller-namespace kube-system \
#        < /tmp/workout-service-secret.yaml \
#        > microservices/workout-service/templates/sealed-secret.yaml
#   3. rm /tmp/workout-service-secret.yaml
# =============================================================================
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: workout-service-secret
  namespace: fit-forge
  labels:
    app: workout-service
    app.kubernetes.io/part-of: fitforge
spec:
  encryptedData:
    MONGO_URI: ""
    JWT_SECRET: ""
  template:
    metadata:
      name: workout-service-secret
      namespace: fit-forge
      labels:
        app: workout-service
    type: Opaque
